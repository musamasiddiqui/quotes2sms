This script uses two dependencies:

- Twilio
- FeedParser

Both can be installed using pip:

pip install twilio
pip install feedparser

To use this script, you need to get a FREE Twilio account that will enable you to send free sms to your cellphone.